import secrets

tokens = {}

def generate_token(user_id):
    token = secrets.token_hex(16)
    tokens[token] = user_id
    return token


def verify_token(token):
    if token in tokens:
        return True
    return False